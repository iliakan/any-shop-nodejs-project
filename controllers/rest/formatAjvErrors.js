// NOT USED
/*
module.exports = function(errors) {
  let formattedErrors = {};
  for (let error of errors) {
    if (keyword === 'required') {
      formattedErrors[error.params.missingProperty] = `Property ${error.params.missingProperty} is required.`;
    } else if (keyword === 'type') {
      formattedErrors[]
    }
  }


};
*/

/*
{
  keyword: 'required',
    dataPath: '',
  schemaPath: '#/required',
  params: { missingProperty: 'title' },
  message: "should have required property 'title'"
},
{
  keyword: 'type',
    dataPath: '.quantity',
  schemaPath: '#/properties/quantity/type',
  params: { type: 'number' },
  message: 'should be number'
}*/
