extends /layouts/main

block append variables
  - let headTitle = error.title || 'Доступ запрещён'
  - let layout_header_class = "main__header_center"

block content
  +b.error
    +e('h1').type= headTitle
    +e.code 403
    +e.text
      if error.info
        != error.info
      else
        | Не хватает прав.
